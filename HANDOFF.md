# העברה ליוסי — דף הנחיתה MindScale

## הדף החי
- **כתובת:** https://yarin-ms.github.io/mindscale-landing/
- מתפרסם אוטומטית מ-GitHub Pages, מהמאגר `yarin-ms/mindscale-landing`, ענף `main`.

## איך מעדכנים
1. עורכים את `index.html` (קובץ יחיד — HTML+CSS+JS סטטי, בלי build).
2. `git add . && git commit -m "..." && git push`
3. הדף מתעדכן תוך כדקה.

## שני המאגרים (מסונכרנים)
- `yarin-ms/mindscale-landing` — **המאגר החי** (מחובר ל-GitHub Pages). יש לך גישת admin.
- `YososSH2009/mindscale-landing` — שלך, גיבוי מסונכרן.
> מומלץ לעבוד על yarin-ms (החי). אם תעדיף שהדף יפורסם מהמאגר שלך — Settings → Pages → Branch: main, וה-URL יהיה YososSH2009.github.io/mindscale-landing.

## ⚠️ הטופס עדיין לא שומר לידים!
שני הטפסים (בראש הדף ובתחתית, `#leadFormHero` ו-`#leadForm`) שולחים ל-`console.log` בלבד — **כל ליד שנכנס נמחק**. חובה לחבר לפני שמפנים תנועה.
- גיליון מוכן: "MindScale — לידים מדף הנחיתה" בחשבון `mindscale.work@gmail.com` (דרייב שיתופי MS TEAM → סוכנות שיווק → מכירות).
- צריך: Apps Script `doPost` שכותב לגיליון + שולח מייל התראה, deploy as web app, ואז לעדכן את ה-`fetch` ב-`index.html`.

## דיוקי קופי שירין עשה (נשמרו רק בדפדפן שלו — לבדוק ולהכניס לקוד)
1. **שורת קהל היעד בראש** — שינה ל: "לנותני שירות, **ליווים וקורסים** שכבר עושים 50,000 ₪+ בחודש מאינסטגרם **:**" _(נראה לא גמור — אולי "מלווים", וה-":" אולי במקום האימוג'י 👈 — תחליט)_
2. **תת-הכותרת בראש** — שינה ל: "**סוכנות שיווק עם** מעטפת שיווק מלאה — תוכן, משפכים וממומן. שכבר הכניסה מעל 10,000,000 ₪ ללקוחות שלנו, בעסקי קורסים, הכשרות וליווי."
3. **שורת ה-USP** — הוסיף בהתחלה: "**המודל:** ריטיינר בסיס נמוך — ואחוז קטן (6.5%) רק על הצמיחה."
4. **כותרת No-Risk** — "כבר נכווית מסוכנות פעם אחת**,** אנחנו יודעים. 👀" (החליף נקודה בפסיק)

## נושאים פתוחים
- **דומיין:** `mindscale.co.il` פנוי לרישום (~60₪/שנה). אחרי רישום: Settings → Pages → Custom domain.
- **כפתור העריכה ✏️** (פינה שמאלית-תחתונה): מטעה — שומר עריכות בדפדפן המקומי בלבד, לא בדף האמיתי. מומלץ להסיר את הבלוק `#edbar` + הסקריפט שלו מ-`index.html`.
